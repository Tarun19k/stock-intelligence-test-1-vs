# GSI Dashboard — Session Learnings Log
# ════════════════════════════════════════════════════════════════════════
#
# PURPOSE: Tracks what each Claude session learned fresh, what context
# was stale or wrong, and any near-miss confusions. Enables detection of:
#   - Hallucinations (Claude invented something not in the codebase)
#   - Stale information (CLAUDE.md / context files lagging reality)
#   - Confusion patterns (Claude misread a file or misapplied a rule)
#   - Genuine new learnings (patterns discovered during the session)
#
# RULES (append-only — same discipline as GSI_AUDIT_TRAIL.md):
#   1. One record per session. Written by Claude at session close.
#   2. Records are never edited. Use type: CORRECTION to amend a prior record.
#   3. Every stale-info finding must note WHICH file was wrong and the fix.
#   4. Hallucinations get flagged even if minor — pattern detection requires
#      complete data.
#   5. Updated by Claude via /log-learnings command (Phase 3 close step).
#
# RECORD TYPES:
#   LEARNING   — new understanding gained that isn't in any doc
#   STALE      — a context file contained outdated information
#   CONFUSION  — Claude misread or misapplied something (near-miss)
#   HALLUCINATION — Claude generated a fact not present in the codebase
#   CORRECTION — amends a prior record (reference by session ID)
#
# FORMAT:
#   ## RECORD-NNN | YYYY-MM-DD | session_NNN | vX.XX | TYPE
#   **Finding:** [what was learned / what was wrong]
#   **Source:** [which file or interaction revealed this]
#   **Impact:** LOW | MEDIUM | HIGH
#   **Fix applied:** [what was corrected, or "none — documentation only"]
# ════════════════════════════════════════════════════════════════════════

---

## RECORD-001 | 2026-04-01 | session_013 | v5.34 | STALE
**Finding:** CLAUDE.md declared regression baseline as 415/415. Actual post-v5.34 baseline is 427/427. Discrepancy of 12: R26 added 5 permanent observability checks; R27 added 7 checks that execute regardless of manifest status (manifest_valid_json + change_log_complete per-file gates do NOT require status == IN_PROGRESS).
**Source:** `python3 regression.py` output vs CLAUDE.md "Regression baseline: 415/415 PASS"
**Impact:** MEDIUM — next session would run regression, see 427, and assume it had broken something by adding 12 phantom checks.
**Fix applied:** CLAUDE.md "Current State" baseline updated from 415 → 427. GSI_CONTEXT.md regenerated to reflect same.

---

## RECORD-002 | 2026-04-01 | session_013 | v5.34 | STALE
**Finding:** GSI_CONTEXT.md header read "ALL 415 CHECKS PASS" after being regenerated by `generate_context.py`. Root cause: `generate_context.py` reads `session['meta']['current_app_version']` and `session['open_items']` from GSI_session.json. That file still had `current_app_version: "v5.32"` and unresolved items (RISK-001, RISK-003, OPEN-017) that were closed in v5.33. The generator produced a v5.32-era context file.
**Source:** `generate_context.py` output during Phase 3 sprint close.
**Impact:** HIGH — GSI_CONTEXT.md is uploaded to Claude Project Files for every session. A stale context file would corrupt every future session's understanding of open items and version state.
**Fix applied:** GSI_session.json `meta.current_app_version` updated to v5.34; resolved open items removed. Context regenerated and verified.

---

## RECORD-003 | 2026-04-01 | session_013 | v5.34 | STALE
**Finding:** GSI_session.json `regression.expected_output` still read "ALL 410 CHECKS PASS" after v5.34 was complete. R18 (session manifest baseline sync check) caught this as a failure: "CLAUDE.md shows 415 but manifest expects 410".
**Source:** R18 regression check failure after Phase 3 updates.
**Impact:** MEDIUM — R18 would block commit until manually corrected each sprint.
**Fix applied:** `regression.expected_output` updated to "ALL 415 CHECKS PASS" (415 was the target at session commit time; subsequent regression revealed actual is 427 — see RECORD-001).

---

## RECORD-004 | 2026-04-01 | session_013 | v5.34 | LEARNING
**Finding:** `sync_docs.py` was never a Phase 3 step — it was run ad-hoc after sprint close. This caused 7 documentation files to be missed in v5.34: CHANGELOG.md, README.md, AGENTS.md, GSI_COMPLIANCE_CHECKLIST.md, .github/PULL_REQUEST_TEMPLATE.md, GSI_DECISIONS.md, GSI_QA_STANDARDS.md.
**Source:** Post-sprint `sync_docs.py --check` run surfacing 4 remaining actions.
**Impact:** HIGH — documentation drift compounds across sprints. Baseline counts in compliance/PR template were one sprint behind.
**Fix applied:** Documented in GSI_WIP.md as session_014 MUST DO FIRST infrastructure fix. CLAUDE.md Rule 2 + Phase 3 protocol to be updated in session_014.

---

## RECORD-005 | 2026-04-01 | session_013 | v5.34 | STALE
**Finding:** dashboard.py contained two P0 violations that pre-existed v5.34 and were not caught by earlier regression checks: (1) `Momentum: {score}/100` still present in `_render_header_static()` — violates DO NOT UNDO rule 12; (2) ROE null guard (`roe_str`) missing — ROE displayed "0.0%" for Indian stocks where yfinance returns None.
**Source:** Compliance script (8-gate check) before push: 6/8 pass, 2 fail.
**Impact:** HIGH — rule 12 violation is a P0 regression; ROE misrepresentation is a data integrity issue.
**Fix applied:** Both violations patched in dashboard.py before push. Compliance confirmed 8/8.

---

## RECORD-006 | 2026-04-01 | session_013 | v5.34 | LEARNING
**Finding:** The sprint manifest Tier A checks (4 checks: version.py, CLAUDE.md, GSI_CONTEXT.md, GSI_SPRINT.md) do not include "always-required secondary docs" — files that must be updated every sprint without exception but have no code dependency to trigger remembering them. This is a structural gap: R27 enforces Tier A completeness, but can only enforce what is listed in Tier A.
**Source:** Post-mortem analysis after 7 files were missed.
**Impact:** HIGH — will repeat every sprint until Tier A is expanded.
**Fix applied:** 5 permanent Tier A checks defined in GSI_WIP.md (session_014 implementation target). Full fix: CLAUDE.md Rule 2 step 3 + Phase 3 protocol.

---

## RECORD-007 | 2026-04-01 | session_013 | v5.34 | LEARNING
**Finding:** `pages/observability.py` defined `render_observability()` but never called it at module level. Streamlit MPA runs page files top-to-bottom — without a module-level call, `/observability` served a blank page with no PIN prompt visible. The gate logic itself (`st.text_input` + Unlock button + `DEV_TOKEN` validation) was correct; the entry point was simply missing.
**Source:** User report ("there's no provision to enter the pin") + code inspection of `pages/observability.py`
**Impact:** HIGH — founder-only observability page was completely inaccessible on both local and Cloud deployments since v5.34.
**Fix applied:** Added `render_observability()` call at module level at end of file. Also created `.streamlit/secrets.toml` locally with `DEV_TOKEN = "gsi-dev-2026"`. CEO must mirror this in Streamlit Cloud secrets before deployment.

---

## Template for new records

```
## RECORD-[NNN] | [YYYY-MM-DD] | [session_NNN] | [vX.XX] | [TYPE]
**Finding:** [one paragraph — what was wrong, what was learned, or what was nearly done wrong]
**Source:** [file, check, command, or interaction that surfaced this]
**Impact:** LOW | MEDIUM | HIGH
**Fix applied:** [exact change made, or "documented only — fix pending session_NNN"]
```
